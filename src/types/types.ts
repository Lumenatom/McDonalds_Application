export type Product = {
    id: number;
    name: string;
    currentPrice: number;
    lastPrice: number;
    image: string;
}
export type Category = {
    id: number;
    name: string;
    image: string;
}